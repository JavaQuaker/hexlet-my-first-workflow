name: Hello Workd!
on: push
jobs:
    build:
          runs-on: ubuntu
          steps:
           - name: Checkout code
             uses: actions/checkout@v3
           - name: Run say hello command
             run: make say-hello                       
